# https://travis-ci.org/phoebe-project/phoebe2-docs

language: python

os: linux
dist: trusty
sudo: required

before_install:
  - sudo apt-get install pandoc
  - pip install jupyter

script: |
  if [ "$TRAVIS_COMMIT_MESSAGE" != "updating .md and .py sources" ] && [ -n $GITHUB_API_KEY ]; then
    jupyter nbconvert --to python tutorials/*ipynb --output-dir=tutorials/py
    jupyter nbconvert --to python examples/*ipynb --output-dir=examples/py
    git remote rm origin
    git remote add origin https://kecnry:${GITHUB_API_KEY}@github.com/phoebe-project/phoebe2-docs.git > /dev/null 2>&1
    git -c user.name='travis' -c user.email='travis'
    git add tutorials/py/*py
    git add examples/py/*py
    git commit -m 'updating .md and .py sources'
    git push -q -f origin $TRAVIS_BRANCH > /dev/null 2>&1
  fi
